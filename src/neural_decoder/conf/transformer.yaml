defaults:
  - config

# Override defaults for Transformer
nUnits: 256
nLayers: 5
dropout: 0.2
lrStart: 0.0005
lrEnd: 0.0001
modelName: "transformerV1"
outputDir: "${hydra:runtime.cwd}/logs/speech_logs/${modelName}"
datasetPath: "/Users/soymilk/Codes/neural_seq_decoder/data/ptDecoder_ctc"

# Transformer specific
nhead: 4
dim_feedforward: 1024
