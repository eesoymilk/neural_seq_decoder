defaults:
  - config

# Override defaults for Conformer
nUnits: 256
nLayers: 4
dropout: 0.2
lrStart: 0.0005
lrEnd: 0.0001
modelName: "conformerV1"
outputDir: "${hydra:runtime.cwd}/logs/speech_logs/${modelName}"
datasetPath: "/Users/soymilk/Codes/neural_seq_decoder/data/ptDecoder_ctc"
