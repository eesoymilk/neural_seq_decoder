defaults:
  - config
  - _self_

# Override defaults for SqueezeFormer
nUnits: 256
nLayers: 5
dropout: 0.2
lrStart: 0.0005
lrEnd: 0.0001
modelName: "squeezeformerV1"
outputDir: "${hydra:runtime.cwd}/logs/speech_logs/${modelName}"
datasetPath: "/home/soymilk/neural_seq_decoder/data/ptDecoder_ctc"
